/** @desc This needs to point at the API root. Updating this to i.e /api/v2/ will change all connected Actions */
const API_ROOT = '/api/v1';
export default API_ROOT;

/** @desc This needs to point to the file-server address */
export const FILE_SERVER = 'http://localhost:5000';
